require 'spec_helper'

describe Gleborator do
  it 'has a version number' do
    expect(Gleborator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
